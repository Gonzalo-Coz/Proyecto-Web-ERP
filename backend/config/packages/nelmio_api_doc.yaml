nelmio_api_doc:
    documentation:
        info:
            title: YIGM ERP API
            description: API REST del ERP Yamaha Integral Global Motors
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api/v1
